models:
  det:
    yolo:
      v12l:
        onnx: "model/yolov12l.onnx"
        defaults:
          confidence_threshold: 0.25
          nms_threshold: 0.45
        input_width: 640
        input_height: 640
      v12x:
        onnx: "model/yolov12x.onnx"
        defaults:
          confidence_threshold: 0.25
          nms_threshold: 0.45
        input_width: 640
        input_height: 640
      v8n:
        onnx: "model/yolov8n.onnx"
        input_width: 640
        input_height: 640
  seg:
    yolo:
      v12s_seg:
        onnx: "model/yolov12s-seg.onnx"
        defaults:
          confidence_threshold: 0.25
          nms_threshold: 0.45
        input_width: 640
        input_height: 640
      v8s_seg:
        onnx: "model/yolov8s-seg.onnx"
        input_width: 640
        input_height: 640
